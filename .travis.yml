language: elixir

sudo: false

elixir:
  - '1.4.5'
  - '1.5.3'
  - '1.6.1'

otp_release:
  - '20.0'

env:
  - MIX_ENV="test"

script:
  - mix compile --warnings-as-errors
  - mix coveralls.travis --max-cases 1
  - mix credo

cache:
  directories:
    - _build
    - deps
